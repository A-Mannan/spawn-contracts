## 1. Shared types, template, and storage

- [x] 1.1 Replace `HarvestSplit`, vesting fields, mutable fee fields, and LP carry fields in `src/types/LaunchTypes.sol` with exact `payoutPlan` and versioned `EconomicConfig`; verify `forge build` succeeds and ABI inspection shows no removed fields.
- [x] 1.2 Update `Bounds.defaultTemplate()` and constructor validation for static 1% trading fee, 10%/75%/20% economic defaults, and immutable 20%/90%/50% caps while preserving 40/55/5 graduation; verify `ProtocolTemplate.t.sol` covers “Default global configuration is published”, “Service-fee cap is enforced”, “Quote creator cap is enforced”, “Token-fund cap is enforced”, “Exact cap values are accepted”, and “Trading fee remains immutable”.
- [x] 1.3 Refactor `MilestoneBase` as the sole declaration point for the global protocol ledger and its exact claim-backed subset, economic configuration/version, per-pool pot, per-entry carry/bitmap, creator-path ledger, exact aggregate liability counters, controller/registry references, events, errors, and payout unlock actions; verify all derived contracts declare no mutable state and `make layout-check` passes.
- [x] 1.4 Extend settlement and solvency helpers so exact aggregate counters equal their component ledgers and custody remains class-specific: PoolManager quote claims cover payout pots plus `_protocolClaimBacked`, raw ETH covers carry plus creator-path/direct claimables and the non-claim-backed protocol remainder, and combined custody covers total ETH liabilities; verify “Aggregate liabilities equal component ledgers”, “Custody classes cover their liabilities”, and “Protocol claim redeems only protocol backing” plus harness tests proving claims cannot spend pot/carry reserves.
- [x] 1.5 Remove production callers and surfaces for `FeeLib`, dynamic fee updates, vesting release, inline harvest buyback/donation, and post-graduation LP compounding; verify repository search and compilation show no obsolete production entry point, event, field, or caller remains.

## 2. Append-only plugin registry

- [x] 2.1 Implement `PayoutPluginRegistry` with 256 stable indices and immutable plugin address, take, gas limit, codehash, and role plus reversible suspension; verify `PayoutPluginRegistry.t.sol` covers “Registration appends at a stable index”, “Registered terms cannot change”, “Registry is bounded to one word”, and “Suspension preserves identity”.
- [x] 2.2 Enforce controller-only mutation, duplicate/invalid/code-identity/proxy-like rejection, selectable-role rules, and execution-time hook-global guard checks before registry-only mutation; verify registry tests cover “Unauthorized registry mutation is rejected” and “Registry mutation is blocked during payout delivery” and plan tests cover invalid non-payout entries.
- [x] 2.3 Implement suspension/reactivation and permanent codehash-mismatch resolution for historical plans, redirecting current share and existing carry to creator-path entitlement; verify tests cover “Suspended value redirects to the creator”, “Reactivation affects later allocations only”, and “Codehash mismatch permanently redirects value”.

## 3. Typed governance and global economics

- [x] 3.1 Implement `ProtocolController` with distinct administrator and protocol recipient authorities and two-step propose/accept administrator transfer; verify `EconomicGovernance.t.sol` covers “Administrator and recipient are independent”, “Administrator transfer requires acceptance”, “Unauthorized acceptance is rejected”, and “Administrator has no implicit claim right”.
- [x] 3.2 Implement typed schedule/cancel/execute operations whose identity binds action, complete parameters, controller, chain ID, and salt and whose ready timestamp is captured at scheduling; verify tests cover “Scheduled operation can be cancelled” and “Operation identity binds complete parameters”.
- [x] 3.3 Implement zero initial delay and delayed delay updates under the old captured delay; verify tests cover “Zero-delay update can execute immediately”, “Positive delay is enforced”, “Delay update uses the old delay”, and “Queued readiness does not change retroactively”.
- [x] 3.4 Implement controller-authorized complete economic-config replacement and recipient updates with monotonically increasing versions; verify tests cover “Valid update affects every pool prospectively”, “Prior accrual is not repartitioned”, and “Recipient update transfers unclaimed entitlement”.
- [x] 3.5 Emit complete governance/economic attribution and reject direct hook or registry setters from non-controller callers; verify event and authorization tests pass for every typed action.

## 4. Signed immutable payout plans

- [x] 4.1 Update `LaunchSignature` EIP-712 and deadline-independent config hashing to bind all payout-plan bits while retaining creator-bound CREATE2 salt; verify `LaunchSignature.t.sol` covers “Relayer launches for the signer”, “Creator launches directly”, “Relayer cannot alter any field”, “Relayer cannot alter a plan bit”, “Replay is rejected”, and “Expired signature is rejected”.
- [x] 4.2 Refactor launch validation to resolve set bits, reject unknown/suspended/non-payout entries, cap popcount at eight, and cap take sum at `WAD`; verify `LaunchConfigLib.t.sol` and `PayoutPlans.t.sol` cover “Invalid plan bits are rejected”, “Excessive fixed takes are rejected”, “Enabled plugin count is bounded”, “Unknown or suspended plan bit is rejected”, “Enabled takes above one whole are rejected”, and “More than eight plugins is rejected”.
- [x] 4.3 Store only the exact bitset in immutable per-pool launch state and expose plan views; verify tests cover “Set bits select stable registry indices”, “Plan cannot change after launch”, “Registry growth cannot reinterpret a plan”, and “Launch identity is isolated”.
- [x] 4.4 Support empty and exact-whole plans with the creator as implicit remainder; verify tests cover “Empty plan pays the creator”, “Creator receives allocation dust”, “Empty plan is accepted”, and “Exact whole plan is accepted”.
- [x] 4.5 Preserve off-chain preset semantics and deterministic identity; verify tests cover “Preset names are off chain”, “Preset names are absent”, “Address is predictable”, “Different signer cannot occupy the address”, “Re-signing preserves the address”, and “Different payout plan changes the address”.

## 5. Static-fee launch and immediate dev buy

- [x] 5.1 Build every `PoolKey` with the immutable static 1% fee and no dynamic flag or mutable fee state; verify permission and launch tests cover “Deployed hook has required callback flags”, “Pool has no dynamic-fee flag”, “Pool initialization is restricted”, “Starting price matches anchored FDV”, “Pool uses static one percent”, and “Pool is tradable at launch”.
- [x] 5.2 Replace `FeeStepDown.t.sol` with static-lifetime coverage for “One percent applies from genesis forever”, “Time does not affect the fee”, “Milestones do not affect the fee”, “Governance cannot change the trading fee”, and “Pool has no dynamic fee state”.
- [x] 5.3 Remove dev-buy vesting and deliver a creator-direct dev buy completely before launch returns while relayed launches leave inventory on curve; verify `DevBuy.t.sol` covers “Dev buy consumes curve inventory”, “Dev buy is capped at ten percent”, “Dev buy requires creator transaction”, “No dev buy is the default”, “Dev buy is observable”, “Tokens are delivered fully at launch”, and “No vesting state is created”.
- [x] 5.4 Preserve fixed-supply standard ERC20 behavior through the launch refactor; verify token tests cover “Protocol initially receives the full minted supply”, “Supply is fixed after launch”, and “Token remains standard ERC20”.

## 6. Harvest-to-pot accounting

- [x] 6.1 Refactor `_harvestBand` to retire each completed band, mint claim-backed quote, record gross pool/index attribution, snapshot one economic version, credit the service fee to both the global protocol ledger and `_protocolClaimBacked`, and credit the exact remainder only to the source pot; verify `MilestoneHarvest.t.sol` covers “Crossing the band top completes and accounts for the milestone”, “Gross harvest is attributed before deductions”, “Active service fee is applied”, “Net harvest funds only its source pool”, and “Harvest accounting conserves the gross amount”.
- [x] 6.2 Preserve partial-fill, fee folding, completion idempotence, ascending multi-harvest, and work-cap behavior; verify harvest suites cover “Partial fill does not complete the milestone”, “Band swap fees fold into the gross harvest”, “A sweeping swap accounts for every completed band within the cap”, “Harvests beyond the cap remain pending”, and “A completed band cannot be accounted again”.
- [x] 6.3 Remove all destination work from swap callbacks while returning token residue to ladder carry; verify tests cover “Harvest invokes no payout plugin”, “Harvest proceeds are quote only”, “Harvest leaves direct creator revenue unchanged”, and “Harvest performs no direct destination work”.
- [x] 6.4 Add per-pool pot isolation, multi-milestone aggregation, and claim-backed mid-swap solvency for both net pots and protocol service fees; verify `PayoutPots.t.sol` covers “Gross harvest is attributable”, “Service fee precedes pot funding”, “Pots are isolated across pools”, “Multiple milestones aggregate without losing history”, and “Mid-swap pot and service-fee accrual remain solvent”.
- [x] 6.5 Preserve graduated-phase swap liveness and suppress automatic payout work; verify lifecycle tests cover “Swaps succeed in both directions”, “In-band oscillation cannot prevent completion”, “Ordinary swaps never flush”, “Ordinary swaps do not flush”, and “Plugin availability cannot block a swap”.

## 7. Payout satellite and whole-pot flush

- [x] 7.1 Add `MilestonePayoutPaths` with matching immutables, `onlyDelegated` on every mutating entry point, and a dedicated payout-pot redemption unlock action; verify `make layout-check`, constructor mismatch tests, and `PayoutFlush.t.sol` cover “Any address can flush one pool” and “Whole new pot is redeemed once”.
- [x] 7.2 Snapshot and zero the new pot before interaction, tip exactly 1% of only the newly redeemed net pot, and handle empty/carry-only flushes without unnecessary unlocks; ordinary tip-transfer failure MUST revert the complete flush atomically; verify tests cover “Flusher receives one percent of the net new pot”, “Protocol service fee is never tipped”, “Carry is not tipped twice”, “Carry-only flush remains available”, “Empty flush is a no-op”, and “Ordinary flusher tip failure is atomic”.
- [x] 7.3 Iterate enabled indices in ascending order, compute plugin shares from one distributable snapshot, and pass only `(poolId, token)` plus each allocation; verify `PayoutFailureIsolation.t.sol` covers “Plugins execute deterministically” and “Plugin receives only its allocation”.
- [x] 7.4 Clear carry before each bounded-gas call; define `CALL_FIXED_GAS = 15_000`, `POST_CALL_GAS = 100_000`, `FINALIZE_GAS = 100_000`, and `MAX_PLUGIN_CALL_GAS = 500_000`; apply the exact timed EIP-150 preflight; and treat only the EVM CALL success bit as success while ignoring all returndata; verify tests cover “Void callback success ignores returndata”, “EIP-150 preflight preserves finalization gas”, “Insufficient preflight gas reverts the flush”, “Reverting plugin does not block later plugins”, “Gas-exhausting plugin is isolated”, “Failed carry is retried”, and “Successful delivery is not replayed”.
- [x] 7.5 Credit the creator exact post-tip remainder and redirected suspended/code-invalid shares only after plugin attempts, update aggregate liabilities throughout, and emit complete attribution; verify tests cover “Flush accounting conserves value”, “Empty plan pays the creator”, “Creator receives allocation dust”, and codehash-mismatch redirect behavior.

## 8. Creator and protocol revenue claims

- [x] 8.1 Preserve a per-pool direct creator ledger exclusively for graduation and quote-fee sources; verify `Claims.t.sol` covers “Fee creator share accrues directly”, “Bonding curve creator share accrues directly”, “Token fees never accrue to a claimant”, “Direct creator sources aggregate”, “Milestone harvest does not credit direct creator revenue”, and “Plugin-pot proceeds remain separate”.
- [x] 8.2 Keep direct claims pull-based and current-RevenueNFT-holder authorized, with checks-effects-interactions and no implicit flush; verify tests cover “Current holder claims direct revenue”, “Non-holder direct claim is rejected”, “Empty direct claim is harmless”, “Reentrant claim cannot exceed the ledger”, “New direct accrual remains claimable”, “Direct claim does not flush”, and “Plugin failure cannot block a direct claim”.
- [x] 8.3 Make NFT ownership govern both direct and creator-path entitlement without transfer hooks that settle value; verify transfer tests cover “New holder claims pre-transfer direct revenue”, “New holder receives future direct accruals”, “New holder receives unpaid creator-path value”, “Previous holder loses all creator claim rights”, and “Transfer performs no settlement”.
- [x] 8.4 Implement creator-path claiming as flush-first, recheck RevenueNFT ownership after plugin interactions, and then pull-pay its separate ledger; ownership change MUST revert the whole call, while final recipient failure MUST restore the complete attempted entitlement including self-flush tip and report `(success, attemptedAmount)` plus an event; verify `CreatorPathPayout.t.sol` covers “Arbitrary flush records rather than pushes creator value”, “Creator value follows NFT ownership”, “Creator-path and direct ledgers remain separate”, “Creator payout flushes first”, “Creator self-flush preserves the tip”, “Ownership change during plugins reverts payout”, and “Failed recipient transfer preserves entitlement”.
- [x] 8.5 Consolidate harvest service fees, quote-fee protocol remainder, and graduation protocol share into one global ledger plus its exact claim-backed subset, claimable only by the current recipient through exact subset redemption; verify tests cover “Every protocol source accrues globally”, “Multiple pools aggregate”, “Current recipient claims globally”, “Protocol backing classes remain explicit”, “Protocol claim leaves payout backing intact”, “Unauthorized protocol claim is rejected”, “Administrator has no implicit claim right”, “Recipient update transfers unclaimed entitlement”, and “No per-pool protocol claim exists”.
- [x] 8.6 Enforce liability isolation across pools, parties, inventory, liquidity, pots, and carry; verify adversarial claim tests cover “Claims cannot reach ladder inventory”, “Claims cannot reach locked liquidity”, “Direct creator and protocol claims are isolated”, “Creator claims are isolated across pools”, “Claims cannot reach pot or carry reserves”, and “Protocol claim cannot consume creator value”.
- [x] 8.7 Prove direct revenue remains independently available through payout state transitions; verify tests cover “Direct revenue is claimable while a pot is unflushed”, “Flush leaves direct creator revenue unchanged”, and “Failed delivery leaves direct revenue unchanged”.

## 9. Global payout-delivery protection

- [x] 9.1 Extend `TransientLock` with a protocol-global payout-delivery guard that blocks nested flushes, claims, fee collection, graduation, and governance execution across every pool while allowing callbacks to detect and suppress work; verify `PayoutReentrancy.t.sol` covers “Same-pool reentry is rejected”, “Cross-pool reentry is rejected”, and “Nested callbacks suppress protocol work”.
- [x] 9.2 Add malicious plugins that attempt cross-pool/global drains, recursive callbacks, and post-call guard poisoning; verify tests cover “Unrelated value is unreachable” and “Guard clears after the transaction”.
- [ ] 9.3 Ensure pot/carry effects precede interactions, plugins receive no approvals, and all external payout methods preserve aggregate solvency; verify invariant and unit tests exercise reverting receivers, self-destruct/codehash changes, recursive hooks, and gas griefing.

## 10. Reference buyback and helper composition

- [x] 10.1 Implement hook-authenticated `BuybackAndBurnPlugin` bound to the source pool, spending only delivered ETH through its own bounded cold PoolManager swap and burning every token received; verify `BuybackPlugin.t.sol` covers “Buyback spends only delivered ETH” and “Buyback burns acquired tokens”.
- [x] 10.2 Make failed bounded buy/burn atomic so the hook records the entire attempted value as carry, and make zero delivery a no-op; verify tests cover “Failed buyback carries the entire share” and “Zero delivery performs no swap”.
- [x] 10.3 Implement `SwapAndFlushHelper` as a non-selectable utility that settles the user swap completely before a separate flush, cannot alter route or payout parameters, and forwards output/refunds/tip; verify `SwapAndFlushHelper.t.sol` covers “Opted-in caller swaps then flushes”, “Helper uses a separate flush unlock”, “Helper cannot alter routing”, and “Helper forwards the tip”.
- [x] 10.4 Preserve a settled swap when any later plugin delivery fails by recording carry instead of reverting helper execution; verify “Plugin failure does not undo the settled swap”.
- [x] 10.5 Bootstrap the canonical plan with only the buyback plugin at `floor(2 * WAD / 9)` and creator remainder; verify tests cover “Canonical bits select intended destinations”, “Canonical economics match their declared baseline”, “Preset naming cannot change identity”, and “Preset bit changes alter identity”.

## 11. Swap-fee collection and token routing

- [x] 11.1 Refactor permissionless `collectFees` to use one collection-time economic snapshot, split quote fees by active creator share with exact protocol remainder, and credit no pot or plugin; verify `SwapFees.t.sol` covers “Default quote split is 75 25”, “Active quote distribution applies globally”, “Collection uses one configuration snapshot”, and “Routed amounts conserve each currency”.
- [x] 11.2 Route token fees without swaps or claimant credit: clamp the active milestone-fund share to `max((maxFeeFundedBands - feeFundedBandsCreated) * perBand * bandInventoryCapMultiple - carriedInventory - milestoneFundAccrued, 0)` for still-undeployed fee-funded extension bands and burn the exact same-collection remainder, switching to 100% burn once capacity is exhausted; verify `MilestoneFund.t.sol` covers “Default token routing funds 20 and burns 80”, “Active token distribution applies globally”, “Token fees never credit a claimant or pot”, “Quote fees are never diverted”, “Diversion causes no price impact”, “Diversion stops at the cap”, “Diversion is clamped to remaining extension capacity”, and “Post-cap token fees burn entirely”.
- [x] 11.3 Remove quote/token LP compounding and LP carry while preserving the original full-range position; verify fee and graduation tests cover “Collected fees never compound”, “No fee LP carry exists”, “Quote fees do not compound”, “Token fees do not compound”, “Payouts do not compound”, “No LP fee carry exists”, and “Other routing preserves existing liquidity”.
- [x] 11.4 Preserve bounded permissionless collection, zero-accrual no-op behavior, ladder-fee folding, cold-path burn execution, and caller non-directionality; verify tests cover “Any address can collect”, “Net locked position is preserved”, “Repeated collection is harmless”, “Zero-accrual collection is a no-op”, “Ladder fees remain part of harvest”, “Token burning is cold-path only”, and “Collector cannot redirect value”.
- [x] 11.5 Verify uncollected fees use the collection-time configuration while already credited ledgers/pots retain their original partition; cover “Uncollected fees use collection-time configuration” together with version-attribution assertions.

## 12. Graduation and locked liquidity

- [x] 12.1 Preserve immutable 40/55/5 graduation, moving only the protocol portion to the global ledger and retaining creator pull-payment plus ladder token allocation; verify `Graduation.t.sol` covers “Default split is applied”, “Protocol graduation revenue accrues globally”, “Economic updates do not alter graduation split”, “Curve tokens become ladder inventory”, “Graduation allocations conserve proceeds”, and “Creator proceeds are not pushed”.
- [x] 12.2 Seed both sides of the initial full-range position from original graduation allocations and prohibit every later positive or negative full-range liquidity mutation; verify “Graduation seeds both sides from original allocations”, fee/payout no-compounding scenarios, `make lock-check`, and source inspection of all `modifyLiquidity` sites.
- [ ] 12.3 Preserve manual and automatic graduation under payout guard semantics and ensure payout/plugin availability cannot block graduated swaps; verify graduation, swap-liveness, and nested-callback tests pass.

## 13. Fixtures, harnesses, and adversarial mocks

- [x] 13.1 Rewire `Fixtures.sol`, `HarnessFixtures.sol`, and `ForkFixtures.sol` to deploy registry, controller, launch support, cold paths, payout paths, and the mined hook with matching immutable constructor inputs; verify template/registry/controller mismatch deployment tests fail closed.
- [x] 13.2 Extend launch helpers and signing utilities for exact payout plans, canonical registry bootstrap, global economics, and immediate dev buy; verify existing suites can launch direct, relayed, empty-plan, canonical-plan, and multi-plugin pools without fixture-local protocol duplication.
- [x] 13.3 Extend `MilestoneHookHarness` with test-only actions numbered from 200 for pots, carry, global liabilities, economic versions, and payout redemption, delegating production actions to `super`; verify harness compilation and layout checks.
- [x] 13.4 Add payout mocks for success with empty/malformed/non-empty returndata, explicit revert, gas exhaustion, rejecting ordinary-tip and creator-path recipients, same/cross-pool reentry, callback nesting, code-identity invalidation, RevenueNFT ownership change during callback, and attempted drain; verify each mock is exercised by a named adversarial scenario rather than existing unused.

## 14. Unit-suite migration and scenario completeness

- [x] 14.1 Rewrite affected existing unit suites (`Launch`, `LaunchSignature`, `LaunchConfigLib`, `DevBuy`, `MilestoneHarvest`, `MilestoneMultiHarvest`, `Settlement`, `SwapFees`, `MilestoneFund`, `Claims`, `Graduation`, `ProtocolTemplate`, permissions, smoke, curve retirement, band ownership, ladder cap, and transient lock) to remove vesting, dynamic-fee, old harvest-split, LP-compounding, and per-pool protocol assumptions; verify no obsolete scenario header remains.
- [x] 14.2 Add focused suites `PayoutPluginRegistry.t.sol`, `PayoutPlans.t.sol`, `EconomicGovernance.t.sol`, `PayoutPots.t.sol`, `PayoutFlush.t.sol`, `PayoutFailureIsolation.t.sol`, `PayoutReentrancy.t.sol`, `CreatorPathPayout.t.sol`, `BuybackPlugin.t.sol`, and `SwapAndFlushHelper.t.sol`; verify every payout-plugin scenario name appears literally in a `// --- Scenario: ... ---` header and a passing test.
- [ ] 14.3 Preserve literal scenario-header traceability for all modified capability specs, including “Geometry is not configurable” and “Harvest percentages are not configurable”; generate a scenario-to-test report and fail the completion check for any missing, duplicate-only, renamed, or non-passing scenario.
- [ ] 14.4 Run each affected unit contract individually during migration, then run the full unit layer with `make test`; fix all failures and record the final unit count in the change report.

## 15. Invariant and Base-fork coverage

- [ ] 15.1 Extend `LaunchpadHandler` with bounded actions for multi-pool launch, harvest, flush, carry retry, suspension/reactivation, economic update, fee collection, NFT transfer, creator claims, and global protocol claim while tracking ghost gross/tip/delivery/liability totals.
- [ ] 15.2 Add invariants proving raw ETH plus claims backs aggregate liabilities, pot/carry isolation, gross-harvest and flush conservation, no double tip/delivery, creator remainder/dust ownership, stable plan interpretation, static trading fee, locked full-range liquidity, and token supply conservation; verify `make test-invariant` passes.
- [ ] 15.3 Update Base fork fixtures and fork suites for deployed singleton behavior with static fees, claim-backed harvest pots, dedicated redemption, multi-plugin failure isolation, token burn, global claims, helper composition, and no compounding; verify `make test-fork` passes with `BASE_RPC_URL`.
- [ ] 15.4 Add fork adversarial cases for large sweeping swaps, nested plugin PoolManager interactions, bounded gas failure, codehash mismatch, cross-pool isolation, and collection-time economics; verify the pinned Base block produces the same accounting and callback suppression as unit tests.

## 16. Deployment, tooling, and documentation

- [ ] 16.1 Update deployment scripts to deploy/configure the controller, registry, canonical buyback plugin, helper, launch support, cold paths, and payout paths; register canonical entries deterministically through the bound controller, verify all authorities, immutable terms, economic defaults/caps, codehashes, and template hashes, and complete the exact-pending multisig handoff only after deployment.
- [ ] 16.2 Extend hook-address mining inputs for every finalized immutable satellite/support/registry/controller address, mine an address with exactly the six required callback flags, and deploy only after all addresses are final; verify the deployment smoke test rejects stale salts and mismatched satellites.
- [x] 16.3 Update `tools/check_storage_layout.py`, delegated-entry guard checks, size gates, and lock checks for the payout satellite and no-positive-full-range rule; verify `make layout-check`, `make size`, `make size-gate-selftest`, and `make lock-check` all pass independently.
- [x] 16.4 Update `Makefile` and CI so new unit/invariant/fork suites and all custom gates execute in their correct layers without requiring RPC credentials for local unit runs; verify the CI command matrix locally where credentials permit.
- [x] 16.5 Reconcile `CHANGES.md`, `DESIGN.md`, `CLAUDE.md`, deployment documentation, and the prior launchpad design revision notes with implemented static fees, immediate dev buy, payout plans/pots/plugins, global economics/claims, and no LP compounding; verify repository search finds no normative contradiction or obsolete operational instruction.

## 17. Release validation and rehearsal

- [x] 17.1 Run repository searches proving production removal of `HarvestSplit`, vesting release/state, dynamic fee flags/updates, fee step-downs, inline harvest destinations, LP fee carry/compounding, and per-pool protocol claims; verify remaining matches are only explicit historical/removal documentation or test assertions.
- [ ] 17.2 Run `make fmt`, `make fmt-check`, `make build`, `make pins`, `make size`, `make size-gate-selftest`, `make lock-check`, `make layout-check`, `make test`, and `make test-invariant`; record exact results and bytecode headroom in a release-check report.
- [ ] 17.3 With `BASE_RPC_URL`, run the complete fork campaign and reconcile every delta-spec scenario against a passing unit, invariant, or fork test; verify all 215 scenarios derived by `tools/check_scenarios.py` from the six delta specs are represented and no scenario is marked complete solely by an unexecuted test.
- [ ] 17.4 Perform a local deployment dry-run and Base fork deployment rehearsal, confirming controller ownership handoff, zero initial delay, recipient separation, canonical registration, hook flags, static fee, satellite immutables, predicted token identity, flush/tip/carry, buyback burn, and global claims.
- [ ] 17.5 With `BASE_SEPOLIA_RPC_URL`, funded deployer credentials, and required environment addresses, run the public-testnet rehearsal through launch, trade, harvest, flush, failure/retry, fee collection/burn, NFT transfer, creator claims, governance update, and global protocol claim; record transaction evidence and keep this task open until the external prerequisites and rehearsal succeed.
- [ ] 17.6 Run `make release-check` after all applicable local/fork gates, document any credential-blocked testnet step explicitly, and mark this change complete only when every implementation task and named scenario has passing evidence.